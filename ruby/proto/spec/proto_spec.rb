# frozen_string_literal: true

RSpec.describe Proto do
  it "has a version number" do
    expect(Proto::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
